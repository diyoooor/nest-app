export enum Route {
  AUTH = "auth",
  USERS = "users",
  CATEGORIES = "categories",
}

export enum Services {
  AUTH = "AUTH_SERVICE",
  USERS = "USERS_SERVICE",
  CATEGORIES = "CATEGORIES_SERVICE",
}
