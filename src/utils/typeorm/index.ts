import { Categories } from "./entities/Categories";
import { Session } from "./entities/Session";
import { User } from "./entities/User";
const entities = [User, Session, Categories];

export { User, Session, Categories };
export default entities;
